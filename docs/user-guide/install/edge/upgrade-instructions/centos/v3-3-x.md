---
layout: docwithnav-edge
title: ThingsBoard Edge CE v3.3.x upgrade instructions for CentOS
description: ThingsBoard Edge CE v3.3.x upgrade guide for CentOS
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/edge/upgrade-instructions/'
---

* TOC
{:toc}

## Upgrading Edge CE to 3.3.4.1

{% assign updateServerLink = "centos/v3-3-x/#upgrading-thingsboard-ce-to-3341" %}
{% assign previousVersion = "3.3.4" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://github.com/thingsboard/thingsboard-edge/releases/download/v3.3.4.1/tb-edge-3.3.4.1.rpm
```
{: .copy-code}

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo rpm -Uvh tb-edge-3.3.4.1.rpm
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}

