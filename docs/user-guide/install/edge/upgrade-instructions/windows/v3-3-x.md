---
layout: docwithnav-edge
title: ThingsBoard Edge CE v3.3.x upgrade instructions for Windows
description: ThingsBoard Edge CE v3.3.x upgrade guide for Windows
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/edge/upgrade-instructions/'
---

* TOC
{:toc}

## Upgrading Edge CE to 3.3.4.1

{% assign updateServerLink = "windows/v3-3-x/#upgrading-thingsboard-ce-to-3341" %}
{% assign previousVersion = "3.3.4" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-3.3.4.1 zip](https://github.com/thingsboard/thingsboard-edge/releases/download/v3.3.4.1/tb-edge-windows-3.3.4.1.zip).

### Stop the Edge service

```bash
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the ThingsBoard Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the service

```bash
net start tb-edge
```
{: .copy-code}

