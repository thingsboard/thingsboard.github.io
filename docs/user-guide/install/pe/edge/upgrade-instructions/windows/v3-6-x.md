---
layout: docwithnav-pe-edge
title: ThingsBoard Edge PE v3.6.x upgrade instructions for Windows
description: ThingsBoard Edge PE v3.6.x upgrade guide for Windows
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/pe/edge/upgrade-instructions/'
---

* TOC
{:toc}

## Upgrading Edge PE to 3.6.4

{% assign updateServerLink = "windows/v3-6-x/#upgrading-thingsboard-pe-to-364" %}
{% assign previousVersion = "3.6.3" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-3.6.4pe.zip](https://dist.thingsboard.io/tb-edge-windows-3.6.4pe.zip).

Stop the Edge service:

```text
net stop tb-edge
```
{: .copy-code}

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

Extract and update configuration:
* Extract the ThingsBoard Edge package.
* Compare your old configuration files (from the backup) with the new ones.
* Merge any custom settings into the new configuration files.

### Run the upgrade script

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

**Note:** The upgrade script must be run with Administrator privileges.

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}


## Upgrading Edge PE to 3.6.3

{% assign updateServerLink = "windows/v3-6-x/#upgrading-thingsboard-pe-to-363" %}
{% assign previousVersion = "3.6.2" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-3.6.3pe.zip](https://dist.thingsboard.io/tb-edge-windows-3.6.3pe.zip).

Stop the Edge service:

```text
net stop tb-edge
```
{: .copy-code}

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

Extract and update configuration:
* Extract the ThingsBoard Edge package.
* Compare your old configuration files (from the backup) with the new ones.
* Merge any custom settings into the new configuration files.

### Run the upgrade script

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

**Note:** The upgrade script must be run with Administrator privileges.

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}

## Upgrading Edge PE to 3.6.2

{% assign updateServerLink = "windows/v3-6-x/#upgrading-thingsboard-pe-to-362" %}
{% assign previousVersion = "3.6.1" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-3.6.2pe.zip](https://dist.thingsboard.io/tb-edge-windows-3.6.2pe.zip).

Stop the Edge service:

```text
net stop tb-edge
```
{: .copy-code}

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

Extract and update configuration:
* Extract the ThingsBoard Edge package.
* Compare your old configuration files (from the backup) with the new ones.
* Merge any custom settings into the new configuration files.

### Run the upgrade script

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

**Note:** The upgrade script must be run with Administrator privileges.

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}


## Upgrading Edge PE to 3.6.1

{% assign updateServerLink = "windows/v3-6-x/#upgrading-thingsboard-pe-to-361" %}
{% assign previousVersion = "3.6" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-3.6.1pe.zip](https://dist.thingsboard.io/tb-edge-windows-3.6.1pe.zip).

Stop the Edge service:

```text
net stop tb-edge
```
{: .copy-code}

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

Extract and update configuration:
* Extract the ThingsBoard Edge package.
* Compare your old configuration files (from the backup) with the new ones.
* Merge any custom settings into the new configuration files.

### Run the upgrade script

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

**Note:** The upgrade script must be run with Administrator privileges.

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}


## Upgrading Edge PE to 3.6.0

{% assign updateServerLink = "windows/v3-6-x/#upgrading-thingsboard-pe-to-36" %}
{% assign previousVersion = "3.5.1.1" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-3.6pe.zip](https://dist.thingsboard.io/tb-edge-windows-3.6pe.zip).

Stop the Edge service:

```text
net stop tb-edge
```
{: .copy-code}

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

Extract and update configuration:
* Extract the ThingsBoard Edge package.
* Compare your old configuration files (from the backup) with the new ones.
* Merge any custom settings into the new configuration files.

### Run the upgrade script

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

**Note:** The upgrade script must be run with Administrator privileges.

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}

