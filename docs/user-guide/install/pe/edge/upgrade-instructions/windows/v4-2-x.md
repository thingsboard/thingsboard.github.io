---
layout: docwithnav-pe-edge
title: ThingsBoard Edge PE v4.2.x upgrade instructions for Windows
description: ThingsBoard Edge PE v4.1.x upgrade guide for Windows
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/pe/edge/upgrade-instructions/'
---

* TOC
{:toc}

## Upgrading Edge PE to 4.2.1.1

{% assign updateServerLink = "windows/v4-2-x/#upgrading-thingsboard-pe-to-4211" %}
{% assign previousVersion = "4.2.1" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-4.2.1.1pe.zip](https://dist.thingsboard.io/tb-edge-windows-4.2.1.1pe.zip).

### Stop the Edge service

```text
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the ThingsBoard Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```text
C:\tb-edge>upgrade.bat 
```
{: .copy-code}

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}

## Upgrading Edge PE to 4.2.1

{% assign updateServerLink = "windows/v4-2-x/#upgrading-thingsboard-pe-to-421" %}
{% assign previousVersion = "4.2.0" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-4.2.1pe.zip](https://dist.thingsboard.io/tb-edge-windows-4.2.1pe.zip).

### Stop the Edge service

```text
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the ThingsBoard Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```text
C:\tb-edge>upgrade.bat 
```
{: .copy-code}

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}

## Upgrading Edge PE to 4.2.0

{% assign updateServerLink = "windows/v4-2-x/#upgrading-thingsboard-pe-to-42" %}
{% assign previousVersion = "4.1.0" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download ThingsBoard Edge package for Windows: [tb-edge-windows-4.2pe.zip](https://dist.thingsboard.io/tb-edge-windows-4.2pe.zip).

### Stop the Edge service

```text
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your ThingsBoard Edge configuration located in **\<ThingsBoard Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the ThingsBoard Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```text
C:\tb-edge>upgrade.bat 
```
{: .copy-code}

### Start the Edge service

```text
net start tb-edge
```
{: .copy-code}


