---
layout: docwithnav-pe-edge
title: ThingsBoard Edge PE v4.2.x upgrade instructions for Ubuntu
description: ThingsBoard Edge PE v4.2.x upgrade guide for Ubuntu
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/pe/edge/upgrade-instructions/'
---

* TOC
{:toc}

## Upgrading Edge PE to 4.2.1 (4.2.1.2)

{% assign updateServerLink = "ubuntu/v4-2-x/#upgrading-thingsboard-pe-to-421-4212" %}
{% assign previousVersion = "4.2.1" %}
{% assign peDocsPrefix = "pe/" %}
{% assign minorVersion = "4.2.0" %}
{% assign mainteneceVersion = "4.2.1.x" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-4.2.1.2pe.deb
```
{: .copy-code}

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-4.2.1.2pe.deb
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

{% include templates/edge/install/info-patch-upgrade.md %}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}

## Upgrading Edge PE to 4.2

{% assign updateServerLink = "ubuntu/v4-2-x/#upgrading-thingsboard-pe-to-42" %}
{% assign previousVersion = "4.1.0" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-4.2pe.deb
```
{: .copy-code}

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-4.2pe.deb
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh 
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}
