---
layout: docwithnav-pe-edge
title: ThingsBoard Edge PE v3.6.x upgrade instructions for Ubuntu
description: ThingsBoard Edge PE v3.6.x upgrade guide for Ubuntu
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/pe/edge/upgrade-instructions/'
---

* TOC
{:toc}

## Upgrading Edge PE to 3.6.4

{% assign updateServerLink = "ubuntu/v3-6-x/#upgrading-thingsboard-pe-to-364" %}
{% assign previousVersion = "3.6.3" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-3.6.4pe.deb

```
{: .copy-code}

### Stop the Edge service

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-3.6.4pe.deb

```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

### Run the upgrade script

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}


## Upgrading Edge PE to 3.6.3

{% assign updateServerLink = "ubuntu/v3-6-x/#upgrading-thingsboard-pe-to-363" %}
{% assign previousVersion = "3.6.2" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-3.6.3pe.deb
```
{: .copy-code}

### Stop the Edge service

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-3.6.3pe.deb
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

### Run the upgrade script

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}


## Upgrading Edge PE to 3.6.2

{% assign updateServerLink = "ubuntu/v3-6-x/#upgrading-thingsboard-pe-to-362" %}
{% assign previousVersion = "3.6.1" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-3.6.2pe.deb
```
{: .copy-code}

### Stop the Edge service

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-3.6.2pe.deb
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

### Run the upgrade script

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}


## Upgrading Edge PE to 3.6.1

{% assign updateServerLink = "ubuntu/v3-6-x/#upgrading-thingsboard-pe-to-361" %}
{% assign previousVersion = "3.6.0" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-3.6.1pe.deb
```
{: .copy-code}

### Stop the Edge service

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-3.6.1pe.deb
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

### Run the upgrade script

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}


## Upgrading Edge PE to 3.6.0

{% assign updateServerLink = "ubuntu/v3-6-x/#upgrading-thingsboard-pe-to-36" %}
{% assign previousVersion = "3.5.1.1" %}
{% assign peDocsPrefix = "pe/" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

```bash
wget https://dist.thingsboard.io/tb-edge-3.6pe.deb
```
{: .copy-code}

### Stop the Edge service

Stop the ThingsBoard Edge service before upgrading to prevent file conflicts:

```bash
sudo service tb-edge stop
```
{: .copy-code}

### Upgrade the package

```bash
sudo dpkg -i tb-edge-3.6pe.deb
```
{: .copy-code}

**Note:** The package installer may prompt you to merge configuration files. Select the **merge option** to preserve your existing settings.

### Run the upgrade script

Execute the upgrade script to migrate your data and configurations:

```bash
sudo /usr/share/tb-edge/bin/install/upgrade.sh --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the Edge service

```bash
sudo service tb-edge start
```
{: .copy-code}

